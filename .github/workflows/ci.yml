name: CI

on:
  push:
  pull_request:

jobs:
  core:
    name: Core (Linux)
    runs-on: ubuntu-latest
    container: swift:6.0
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Test
        run: swift test
